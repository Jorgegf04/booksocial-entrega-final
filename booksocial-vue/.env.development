# Variables de entorno para DESARROLLO local
# Este fichero SÍ puede ir al repo (no contiene secretos)

# URL base de la API del backend Spring Boot (puerto 9999 en local)
VITE_API_BASE_URL=http://localhost:9999/api
